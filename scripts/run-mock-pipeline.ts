import { runEvStatusPipeline } from "@/lib/pipeline";

runEvStatusPipeline()
  .then((result) => {
    console.log(JSON.stringify(result, null, 2));
  })
  .catch((error) => {
    console.error(error);
    process.exitCode = 1;
  });
